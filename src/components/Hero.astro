---
import { Image } from "astro:assets";
import logo from "../assets/astro.svg";
---

<section 
  class="hero-section relative w-full min-h-screen pt-20 sm:pt-24 pb-8 sm:pb-10 overflow-hidden transition-colors duration-300"
  role="banner"
  aria-label="Sekcja główna - Frontend Developer Adrian"
>
  <!-- Enhanced gradient background with better performance -->
  <div class="absolute inset-0 bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-100 dark:from-slate-900 dark:via-slate-800 dark:to-slate-900"></div>
  
  <!-- Grid pattern overlay for texture -->
  <div class="absolute inset-0 opacity-[0.02] dark:opacity-[0.05]" style="background-image: radial-gradient(circle at 1px 1px, #000 1px, transparent 0); background-size: 40px 40px;"></div>
  <!-- Enhanced background decoration with better performance -->
  <div class="absolute inset-0 opacity-5 hidden md:block will-change-transform">
    <div class="absolute top-20 left-10 w-72 h-72 bg-gradient-to-r from-blue-400 to-blue-500 rounded-full mix-blend-multiply filter blur-xl animate-blob"></div>
    <div class="absolute top-40 right-10 w-72 h-72 bg-gradient-to-r from-purple-400 to-purple-500 rounded-full mix-blend-multiply filter blur-xl animate-blob animation-delay-2000"></div>
    <div class="absolute -bottom-8 left-20 w-72 h-72 bg-gradient-to-r from-indigo-400 to-indigo-500 rounded-full mix-blend-multiply filter blur-xl animate-blob animation-delay-4000"></div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 2xl:px-4 flex flex-col-reverse lg:flex-row items-center gap-8 lg:gap-12 2xl:gap-16 min-h-[80vh] lg:min-h-[70vh]">
    <!-- Enhanced Text Section -->
    <div class="flex-1 text-center lg:text-left space-y-6 lg:space-y-8 animate-fade-in">
      <!-- Improved availability badge with urgency -->
      <div 
        class="inline-flex items-center px-4 py-2 bg-white/80 dark:bg-slate-800/80 backdrop-blur-md rounded-full border border-blue-200/50 dark:border-blue-400/30 shadow-lg shadow-blue-500/10 animate-bounce-in hover:shadow-xl hover:scale-105 transition-all duration-300 cursor-default"
        role="status"
        aria-label="Status dostępności"
      >
        <span class="w-2 h-2 bg-green-500 rounded-full mr-3 animate-pulse shadow-sm shadow-green-500/50"></span>
        <span class="text-sm font-medium text-gray-700 dark:text-gray-200">🚀 Tylko 2 miejsca w tym miesiącu</span>
      </div>

      <!-- Enhanced main heading with stronger value proposition -->
      <h1 
        class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl xl:text-7xl font-black text-gray-900 dark:text-white leading-[1.1] tracking-tight space-y-2"
        role="heading"
        aria-level="1"
      >
        <span class="block">Zamieniam <span class="relative inline-block group">
          <span class="bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 bg-clip-text text-transparent animate-gradient group-hover:scale-105 transition-transform duration-300">
            odwiedziny
          </span>
          <svg 
            class="absolute -bottom-2 left-0 w-full h-3 text-blue-300 dark:text-blue-400 opacity-70 group-hover:opacity-100 transition-opacity duration-300" 
            viewBox="0 0 100 12" 
            fill="currentColor" 
            preserveAspectRatio="none"
            aria-hidden="true"
          >
            <path d="M0,8 Q25,2 50,6 T100,4 L100,12 L0,12 Z" opacity="0.6"/>
          </svg>
        </span> w</span>
        <span class="block bg-gradient-to-r from-purple-600 via-pink-600 to-red-500 bg-clip-text text-transparent animate-gradient hover:scale-105 transition-transform duration-300 cursor-default">
          sprzedaż
        </span>
      </h1>

      <!-- Enhanced subtitle with concrete value proposition -->
      <p class="text-base sm:text-lg md:text-xl lg:text-2xl text-gray-600 dark:text-gray-300 max-w-2xl leading-relaxed animate-slide-up">
        Tworzę 
        <span class="font-bold text-blue-700 dark:text-blue-400 relative group">
          strony internetowe
          <span class="absolute -bottom-1 left-0 w-full h-0.5 bg-blue-300 dark:bg-blue-500 group-hover:h-1 transition-all duration-300"></span>
        </span> 
      , które pomagają firmom zwiększyć sprzedaż o 40-80%. 
        Łączę nowoczesny design z psychologią sprzedaży i sprawdzonymi technikami konwersji.
      </p>

      <!-- Enhanced stats with more credible and specific numbers -->
      <div 
        class="flex flex-wrap justify-center lg:justify-start gap-6 lg:gap-8 text-center animate-slide-up animation-delay-200"
        role="region"
        aria-label="Statystyki osiągnięć"
      >
        <div class="group cursor-default hover:scale-105 transition-transform duration-300">
          <div class="text-3xl lg:text-4xl font-bold text-blue-600 dark:text-blue-400 group-hover:text-blue-700 dark:group-hover:text-blue-300 transition-colors duration-300">127+</div>
          <div class="text-sm text-gray-600 dark:text-gray-400 font-medium">Projektów</div>
        </div>
        <div class="group cursor-default hover:scale-105 transition-transform duration-300">
          <div class="text-3xl lg:text-4xl font-bold text-purple-600 dark:text-purple-400 group-hover:text-purple-700 dark:group-hover:text-purple-300 transition-colors duration-300">5+</div>
          <div class="text-sm text-gray-600 dark:text-gray-400 font-medium">Lat doświadczenia</div>
        </div>
        <div class="group cursor-default hover:scale-105 transition-transform duration-300">
          <div class="text-3xl lg:text-4xl font-bold text-indigo-600 dark:text-indigo-400 group-hover:text-indigo-700 dark:group-hover:text-indigo-300 transition-colors duration-300">2.8M+</div>
          <div class="text-sm text-gray-600 dark:text-gray-400 font-medium">Wygenerowane PLN</div>
        </div>
      </div>

      <!-- Enhanced CTA buttons with action-oriented copy -->
      <div 
        class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start animate-bounce-in animation-delay-300"
        role="group"
        aria-label="Główne akcje"
      >
        <a 
          href="#projects" 
          class="group relative inline-flex items-center justify-center px-8 py-4 bg-gradient-to-r from-blue-600 via-indigo-600 to-purple-600 text-white font-bold rounded-xl shadow-lg hover:shadow-xl focus:shadow-xl transition-all duration-300 hover:scale-105 active:scale-95 focus:outline-none focus:ring-4 focus:ring-blue-400/50 dark:focus:ring-blue-500/50 overflow-hidden"
          aria-label="Zobacz przykłady stron internetowych"
        >
          <span class="relative z-10 transition-transform duration-300 group-hover:translate-x-1">Zobacz strony</span>
          <svg 
            class="ml-2 w-5 h-5 transition-transform duration-300 group-hover:translate-x-2" 
            fill="none" 
            stroke="currentColor" 
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
          <div class="absolute inset-0 bg-gradient-to-r from-blue-700 via-indigo-700 to-purple-700 opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          <div class="absolute inset-0 bg-white/20 scale-x-0 group-hover:scale-x-100 origin-left transition-transform duration-500 ease-out"></div>
        </a>
        
        <a 
          href="#contact" 
          class="group inline-flex items-center justify-center px-8 py-4 bg-white/90 dark:bg-slate-800/90 backdrop-blur-md text-gray-800 dark:text-gray-200 font-semibold rounded-xl border-2 border-gray-200/50 dark:border-gray-600/50 shadow-lg hover:shadow-xl hover:bg-white dark:hover:bg-slate-800 transition-all duration-300 hover:scale-105 active:scale-95 focus:outline-none focus:ring-4 focus:ring-gray-400/50 dark:focus:ring-gray-500/50"
          aria-label="Umów bezpłatną konsultację"
        >
          <svg 
            class="mr-2 w-5 h-5 transition-transform duration-300 group-hover:scale-110" 
            fill="none" 
            stroke="currentColor" 
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span class="transition-transform duration-300 group-hover:translate-x-1">Bezpłatna konsultacja</span>
        </a>
      </div>
    </div>

    <!-- Enhanced Image Section -->
    <div class="flex-1 flex justify-center lg:justify-end mb-8 lg:mb-0">
      <div class="relative group">
        <!-- Enhanced glowing effect with dark mode support -->
        <div class="absolute inset-0 bg-gradient-to-r from-blue-400 via-purple-400 to-indigo-400 rounded-full blur-3xl opacity-20 dark:opacity-30 scale-110 animate-pulse-glow hidden sm:block group-hover:scale-125 transition-transform duration-700"></div>
        
        <!-- Main image container with improved styling -->
        <div 
          class="relative z-10 p-6 sm:p-8 bg-white/20 dark:bg-slate-800/20 backdrop-blur-md rounded-3xl border border-white/30 dark:border-slate-600/30 shadow-2xl animate-float hidden md:block group-hover:scale-105 transition-all duration-500" 
          style="margin-top:2rem;"
          role="img"
          aria-label="Adrian - twórca stron internetowych"
        >
          <Image 
            src={logo} 
            alt="Adrian - Frontend Developer tworzący strony internetowe w React i Vue.js" 
            class="w-32 sm:w-48 md:w-64 lg:w-72 drop-shadow-2xl transition-transform duration-500 group-hover:rotate-3" 
            loading="eager"
            decoding="async"
          />
        </div>
        
        <!-- Enhanced floating elements with better positioning -->
        <div class="absolute -top-6 -right-6 w-20 h-20 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl opacity-80 animate-bounce-slow hidden sm:block group-hover:rotate-12 transition-transform duration-500 shadow-lg shadow-blue-500/30"></div>
        <div class="absolute -bottom-8 -left-8 w-16 h-16 bg-gradient-to-br from-purple-500 to-purple-600 rounded-full opacity-70 animate-bounce-slow animation-delay-1000 hidden sm:block group-hover:-rotate-12 transition-transform duration-500 shadow-lg shadow-purple-500/30"></div>
        <div class="absolute top-1/2 -right-10 w-12 h-12 bg-gradient-to-br from-indigo-400 to-indigo-500 rounded-lg opacity-70 animate-bounce-slow animation-delay-2000 hidden sm:block group-hover:rotate-45 transition-transform duration-500 shadow-lg shadow-indigo-500/30"></div>
        
        <!-- New: Orbit elements for enhanced visual interest -->
        <div class="absolute top-1/4 -left-12 w-8 h-8 bg-gradient-to-br from-pink-400 to-pink-500 rounded-full opacity-60 animate-orbit hidden lg:block"></div>
        <div class="absolute bottom-1/4 -right-14 w-6 h-6 bg-gradient-to-br from-cyan-400 to-cyan-500 rounded-full opacity-50 animate-orbit-reverse hidden lg:block"></div>
      </div>
    </div>
  </div>

  <!-- Enhanced scroll indicator with better accessibility -->
  <div 
    class="absolute left-1/2 bottom-4 -translate-x-1/2 flex flex-col items-center animate-fade-in z-20 group cursor-pointer"
    role="button"
    tabindex="0"
    aria-label="Zobacz przykłady stron internetowych"
    onclick="document.querySelector('#about, #projects, main > section:nth-child(2)')?.scrollIntoView({ behavior: 'smooth' })"
    onkeydown="if(event.key === 'Enter' || event.key === ' ') { event.preventDefault(); document.querySelector('#about, #projects, main > section:nth-child(2)')?.scrollIntoView({ behavior: 'smooth' }); }"
  >
    <span class="text-xs md:text-sm text-gray-500 dark:text-gray-400 mb-2 opacity-70 group-hover:opacity-100 transition-opacity duration-300">Zobacz projekty</span>
    <div class="w-10 h-10 flex items-center justify-center rounded-full bg-white/20 dark:bg-slate-800/20 backdrop-blur-sm border border-white/30 dark:border-slate-600/30 group-hover:bg-white/30 dark:group-hover:bg-slate-700/30 group-hover:scale-110 transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-blue-400/50">
      <svg 
        class="animate-bounce-slow text-blue-500 dark:text-blue-400 group-hover:text-blue-600 dark:group-hover:text-blue-300 transition-colors duration-300" 
        width="24" 
        height="24" 
        viewBox="0 0 24 24" 
        fill="none" 
        xmlns="http://www.w3.org/2000/svg"
        aria-hidden="true"
      >
        <path d="M12 5v14M12 19l-6-6M12 19l6-6" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </div>
  </div>
</section>
<style>
/* Enhanced keyframe animations with performance optimizations */
@keyframes fade-in {
  from { 
    opacity: 0; 
    transform: translateY(30px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}

@keyframes slide-up {
  from { 
    opacity: 0; 
    transform: translateY(40px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}

@keyframes bounce-in {
  0% { 
    transform: scale(0.8); 
    opacity: 0; 
  }
  60% { 
    transform: scale(1.05); 
    opacity: 1; 
  }
  100% { 
    transform: scale(1); 
  }
}

@keyframes float {
  0%, 100% { 
    transform: translateY(0px) rotate(0deg); 
  }
  33% { 
    transform: translateY(-10px) rotate(1deg); 
  }
  66% { 
    transform: translateY(-5px) rotate(-1deg); 
  }
}

@keyframes blob {
  0%, 100% {
    transform: translate(0px, 0px) scale(1);
    border-radius: 60% 40% 30% 70% / 60% 30% 70% 40%;
  }
  33% {
    transform: translate(30px, -50px) scale(1.1);
    border-radius: 30% 60% 70% 40% / 50% 60% 30% 60%;
  }
  66% {
    transform: translate(-20px, 20px) scale(0.9);
    border-radius: 70% 30% 40% 60% / 40% 70% 60% 50%;
  }
}

@keyframes gradient {
  0%, 100% {
    background-position: 0% 50%;
  }
  50% {
    background-position: 100% 50%;
  }
}

@keyframes bounce-slow {
  0%, 100% {
    transform: translateY(0px) rotate(0deg);
  }
  50% {
    transform: translateY(-20px) rotate(10deg);
  }
}

@keyframes pulse-glow {
  0%, 100% {
    box-shadow: 0 0 20px rgba(59, 130, 246, 0.3);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 40px rgba(59, 130, 246, 0.6), 0 0 60px rgba(147, 51, 234, 0.3);
    transform: scale(1.05);
  }
}

@keyframes orbit {
  0% {
    transform: rotate(0deg) translateX(60px) rotate(0deg);
  }
  100% {
    transform: rotate(360deg) translateX(60px) rotate(-360deg);
  }
}

@keyframes orbit-reverse {
  0% {
    transform: rotate(360deg) translateX(50px) rotate(-360deg);
  }
  100% {
    transform: rotate(0deg) translateX(50px) rotate(0deg);
  }
}

/* New shimmer effect for loading states */
@keyframes shimmer {
  0% {
    transform: translateX(-100%);
  }
  100% {
    transform: translateX(100%);
  }
}

/* New slide-in-from-left animation */
@keyframes slide-in-left {
  from {
    opacity: 0;
    transform: translateX(-50px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* New slide-in-from-right animation */
@keyframes slide-in-right {
  from {
    opacity: 0;
    transform: translateX(50px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* Enhanced animation classes */
.animate-fade-in { 
  animation: fade-in 1s ease-out forwards; 
}

.animate-slide-up { 
  animation: slide-up 1s ease-out forwards; 
}

.animate-bounce-in { 
  animation: bounce-in 1s cubic-bezier(0.68, -0.55, 0.27, 1.55) forwards; 
}

.animate-float { 
  animation: float 6s ease-in-out infinite; 
}

.animate-blob {
  animation: blob 7s infinite;
  will-change: transform, border-radius;
}

.animate-gradient {
  background-size: 200% 200%;
  animation: gradient 3s ease infinite;
}

.animate-bounce-slow {
  animation: bounce-slow 3s ease-in-out infinite;
}

.animate-pulse-glow {
  animation: pulse-glow 2s ease-in-out infinite;
}

.animate-orbit {
  animation: orbit 20s linear infinite;
}

.animate-orbit-reverse {
  animation: orbit-reverse 25s linear infinite;
}

.animate-shimmer {
  animation: shimmer 2s ease-in-out infinite;
}

.animate-slide-in-left {
  animation: slide-in-left 0.8s ease-out forwards;
}

.animate-slide-in-right {
  animation: slide-in-right 0.8s ease-out forwards;
}

/* Enhanced animation delays */
.animation-delay-200 {
  animation-delay: 0.2s;
}

.animation-delay-300 {
  animation-delay: 0.3s;
}

.animation-delay-500 {
  animation-delay: 0.5s;
}

.animation-delay-700 {
  animation-delay: 0.7s;
}

.animation-delay-1000 {
  animation-delay: 1s;
}

.animation-delay-2000 {
  animation-delay: 2s;
}

.animation-delay-4000 {
  animation-delay: 4s;
}

/* Enhanced custom utilities */
.backdrop-blur-sm {
  backdrop-filter: blur(4px);
}

.backdrop-blur-md {
  backdrop-filter: blur(12px);
}

.backdrop-blur-lg {
  backdrop-filter: blur(16px);
}

/* Performance optimizations */
.will-change-transform {
  will-change: transform;
}

.will-change-auto {
  will-change: auto;
}

/* Accessibility improvements */
@media (prefers-reduced-motion: reduce) {
  .animate-fade-in,
  .animate-slide-up,
  .animate-bounce-in,
  .animate-float,
  .animate-blob,
  .animate-gradient,
  .animate-bounce-slow,
  .animate-pulse-glow,
  .animate-orbit,
  .animate-orbit-reverse,
  .animate-shimmer,
  .animate-slide-in-left,
  .animate-slide-in-right {
    animation: none !important;
  }
  
  * {
    transition: none !important;
  }
}

/* Dark mode optimizations */
@media (prefers-color-scheme: dark) {
  .bg-gradient-to-br {
    background: linear-gradient(to bottom right, #0f172a, #1e293b, #0f172a);
  }
}

/* Enhanced responsive improvements */
@media (max-width: 768px) {
  .animate-blob {
    display: none;
  }
  
  .hero-section {
    padding-top: 1.5rem !important;
    min-height: 100vh !important;
  }
  
  h1 {
    font-size: 2.25rem !important;
    line-height: 1.2 !important;
  }
  
  .text-xl, .md\:text-2xl {
    font-size: 1.125rem !important;
    line-height: 1.6 !important;
  }
  
  .gap-8 {
    gap: 1.5rem !important;
  }
  
  .space-y-8 > * + * {
    margin-top: 1.5rem !important;
  }
  
  .px-8 {
    padding-left: 1.5rem !important;
    padding-right: 1.5rem !important;
  }
  
  .py-4 {
    padding-top: 1rem !important;
    padding-bottom: 1rem !important;
  }
}

@media (max-width: 480px) {
  .hero-section .absolute.left-1\/2 {
    bottom: 1rem !important;
  }
  
  h1 {
    font-size: 1.875rem !important;
  }
  
  .text-base {
    font-size: 0.95rem !important;
  }
  
  .gap-4 {
    gap: 0.75rem !important;
  }
  
  .px-8 {
    padding-left: 1.25rem !important;
    padding-right: 1.25rem !important;
  }
}

@media (max-width: 360px) {
  .hero-section .absolute.left-1\/2 {
    display: none !important;
  }
  
  .flex-col {
    gap: 0.5rem !important;
  }
}

/* Container queries for future-proofing */
@container (max-width: 768px) {
  .container-responsive {
    padding: 1rem !important;
  }
}

/* Enhanced focus management */
.focus\:ring-4:focus {
  --tw-ring-offset-shadow: var(--tw-ring-inset) 0 0 0 var(--tw-ring-offset-width) var(--tw-ring-offset-color);
  --tw-ring-shadow: var(--tw-ring-inset) 0 0 0 calc(4px + var(--tw-ring-offset-width)) var(--tw-ring-color);
  box-shadow: var(--tw-ring-offset-shadow), var(--tw-ring-shadow), var(--tw-shadow, 0 0 #0000);
}

/* High contrast mode support */
@media (prefers-contrast: high) {
  .bg-gradient-to-r {
    background: #000 !important;
    color: #fff !important;
  }
  
  .text-gray-600 {
    color: #000 !important;
  }
  
  .border {
    border-width: 2px !important;
    border-color: #000 !important;
  }
}

/* Print styles */
@media print {
  .animate-blob,
  .animate-float,
  .animate-pulse-glow {
    display: none !important;
  }
  
  .shadow-lg,
  .shadow-xl,
  .shadow-2xl {
    box-shadow: none !important;
  }
}

/* Smooth scroll enhancement */
html {
  scroll-behavior: smooth;
}

@media (prefers-reduced-motion: reduce) {
  html {
    scroll-behavior: auto;
  }
}

/* Performance hint for animations */
.animate-blob,
.animate-float,
.animate-orbit,
.animate-orbit-reverse {
  transform: translateZ(0);
  backface-visibility: hidden;
  perspective: 1000px;
}

/* Enhanced loading state */
.loading-shimmer {
  position: relative;
  overflow: hidden;
  background-color: #f3f4f6;
}

.loading-shimmer::after {
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  transform: translateX(-100%);
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.6),
    transparent
  );
  animation: shimmer 1.5s ease-in-out infinite;
  content: '';
}

/* Improved button states */
.btn-primary {
  position: relative;
  overflow: hidden;
  z-index: 1;
}

.btn-primary::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.2),
    transparent
  );
  transition: left 0.5s;
  z-index: -1;
}

.btn-primary:hover::before {
  left: 100%;
}
</style>
    </div>
  </div>
</section>
